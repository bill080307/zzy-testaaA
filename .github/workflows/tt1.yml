name: Test 222

on:
  workflow_dispatch:

jobs:
  build-1:
    runs-on: ubuntu-latest
    steps:
      - name: Show file changes
        run: |
          df -h
          du -hd 2 /usr/local
          du -hd 2 /usr/share
          du -hd 2 /opt/hostedtoolcache
          df -h
          echo "Hello, World!"
